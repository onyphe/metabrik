#!/usr/bin/perl
#
# $Id$
#
use strict;
use warnings;

BEGIN {
   use File::HomeDir;

   my $lib = File::HomeDir->my_home."/Metabricky/lib";
   $lib =~ s/\\/\//g; # Converts Windows path

   eval("use lib \"$lib\";");
   if ($@) {
      chomp($@);
      die("[FATAL] metabricky: unable to load Metabricky libs [$@]\n");
   }
}

use Metabricky::Log::Console;
use Metabricky::Brick::Core::Meby;

my $script = shift;

$Metabricky::Brick::Core::Meby::Log = Metabricky::Log::Console->new(
   level => 3,
);
my $meby = Metabricky::Brick::Core::Meby->new;

if (defined($script)) {
   $meby->cmd("script $script");
}
else {
   $meby->cmdloop;
}
